import { JoinEventDaysPipe } from './join-event-days.pipe';

describe('JoinEventDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinEventDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
